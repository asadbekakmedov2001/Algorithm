#include <iostream>
#include <string.h>

using namespace std;

int main()
{
    char a[100],b[100];
    cin>>a;
    strcpy(b,a);
    strrev(b);
    if(strcmp(a,b)==0)cout<<"Palindrom";
    else cout<<"Palindrom emas";
    return 0;
}
